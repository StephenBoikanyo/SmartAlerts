import 'package:flutter/material.dart';


// iterable data
class CaseCards extends StatelessWidget {
  const CaseCards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Card(

    );
  }
}
